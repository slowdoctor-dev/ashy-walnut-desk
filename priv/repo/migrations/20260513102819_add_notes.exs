defmodule AshyWalnutDesk.Repo.Migrations.AddNotes do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:notes_versions, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :version_action_type, :text, null: false
      add :version_action_name, :text, null: false
      add :version_source_id, :uuid, null: false
      add :changes, :map

      add :version_inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :version_updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
    end

    create table(:notes, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
    end

    alter table(:notes_versions) do
      modify :version_source_id,
             references(:notes,
               column: :id,
               name: "notes_versions_version_source_id_fkey",
               type: :uuid,
               prefix: "public"
             )
    end

    alter table(:notes) do
      add :body, :text, null: false
      add :deleted_at, :utc_datetime_usec

      add :created_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :identity_id,
          references(:identities,
            column: :id,
            name: "notes_identity_id_fkey",
            type: :uuid,
            prefix: "public",
            on_delete: :restrict
          ),
          null: false

      add :recorded_by_id,
          references(:users,
            column: :id,
            name: "notes_recorded_by_id_fkey",
            type: :uuid,
            prefix: "public"
          ),
          null: false
    end

    create index(:notes, [:identity_id, :deleted_at])
  end

  def down do
    drop constraint(:notes, "notes_identity_id_fkey")

    drop constraint(:notes, "notes_recorded_by_id_fkey")

    drop_if_exists index(:notes, [:identity_id, :deleted_at])

    alter table(:notes) do
      remove :recorded_by_id
      remove :identity_id
      remove :updated_at
      remove :created_at
      remove :deleted_at
      remove :body
    end

    drop constraint(:notes_versions, "notes_versions_version_source_id_fkey")

    alter table(:notes_versions) do
      modify :version_source_id, :uuid
    end

    drop table(:notes)

    drop table(:notes_versions)
  end
end
