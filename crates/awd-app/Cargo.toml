[package]
name = "awd-app"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Application/orchestration layer: every Ash action as a transactional function; authorize layer; role-aware DTOs."

[dependencies]
awd-domain = { path = "../awd-domain" }
awd-db = { path = "../awd-db" }
awd-safety = { path = "../awd-safety" }
awd-auth = { path = "../awd-auth" }
serde_json = { workspace = true }
# The transactional orchestration functions (draft::approve, action::execute, …)
# compose repos + jobs + audit appends and need SQLx, deferred. The authorize
# layer + role-aware field redaction below are pure and tested.
